// Glaide de Lara Oliveira
// GRR20184567
#include "graphics.h"

void converte_2d(convert *doisD, vert *vet, FILE *obj)
{
    /*
    xp = xc + zc × ( (xv - xc) ÷ (zv + zc) )
    yp = yc + zc × ( (yv - yc) ÷ (zv + zc) )
    */
}